<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_gray" /> <!-- Màu nền tối -->
    <corners android:radius="8dp" />  <!-- Bo góc cho ô nhập -->
    <stroke android:color="@android:color/transparent" android:width="2dp"/> <!-- Viền trong suốt (hoặc bạn có thể thay đổi màu nếu muốn) -->
</shape>
